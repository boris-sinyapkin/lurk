appenders:
    stdout:
        kind: console
        encoder:
            pattern: "{h({d(%Y-%m-%d %H:%M:%S.%6f %Z)(utc)} | {({l}):5.5} | [{M}])} {m}{n}"

root:
    level: debug
    appenders:
        - stdout